<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ================= LIST ================= -->
    <record id="view_maintenance_request_list" model="ir.ui.view">
        <field name="name">gearguard.maintenance.request.list</field>
        <field name="model">gearguard.maintenance.request</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="equipment_id"/>
                <field name="maintenance_team_id"/>
                <field name="technician_id"/>
                <field name="priority"/>
                <field name="scheduled_date"/>
            </list>
        </field>
    </record>

    <!-- ================= FORM ================= -->
    <record id="view_maintenance_request_form" model="ir.ui.view">
        <field name="name">gearguard.maintenance.request.form</field>
        <field name="model">gearguard.maintenance.request</field>
        <field name="arch" type="xml">
            <form string="Maintenance Request">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="equipment_id"/>
                        <field name="maintenance_team_id"/>
                        <field name="technician_id"/>
                        <field name="priority"/>
                        <field name="scheduled_date"/>
                    </group>

                    <notebook>
                        <page string="Description">
                            <field name="description"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>


    <!-- ================= ACTION ================= -->
    <record id="action_maintenance_request" model="ir.actions.act_window">
        <field name="name">Maintenance Requests</field>
        <field name="res_model">gearguard.maintenance.request</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id"
               ref="view_maintenance_request_search"/>
    </record>

</odoo>
